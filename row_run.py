#!/usr/bin/env python
# * coding: utf8 *
"""
the file to run to start the project
"""
import row

row.main()

# images = row.download("bucket-name", 0)

# for image in images:
#     print(image)
#     row.process(image)
#     row.convert(image)
#     row.rotate(image)
#     row.prepare(image)
#     row.detect(image)
#     row.crop(image)
#     row.prepare(image)
#     row.detect(image)
#     row.write(image)
